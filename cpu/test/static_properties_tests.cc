#include <gtest/gtest.h>

namespace cpu_static_properties_suite {
	TEST(Stack, ReturnsCorrectSize) {
		// stubbed test should be replaced with real one
		EXPECT_EQ(0, 0);
	}
}  // namespace cpu_static_properties_suite
